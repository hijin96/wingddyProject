<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="calendar-mapper">

	<insert id="insertSchedule" parameterType="calendar">
		INSERT
		  INTO
		  	   CALENDAR
		VALUES
			   (
			   SEQ_SCHEDULE_NO.NEXTVAL
			  ,#{memberNo}
			  ,#{schedule}
			  ,#{startDate}
			  ,#{endDate}
			  ,#{color}
			   )  	   
	</insert>

	<select id="selectScheduleList" parameterType="_int" resultType="calendar">
		SELECT
		  	   schedule_no scheduleNo
		  	  ,member_no memberNo
		  	  ,schedule schedule
		  	  ,to_char(start_date, 'YYYY-MM-DD') startDate
		  	  ,to_char(end_date + interval '1' day, 'YYYY-MM-DD') endDate
		  	  ,color color 
		  FROM
		   	   CALENDAR
		 WHERE
		 	   MEMBER_NO = #{memberNo}
	</select>

	<select id="selectDaySchedule" parameterType="hashMap" resultType="calendar" >
		SELECT
			   schedule_no scheduleNo
		  	  ,member_no memberNo
		  	  ,schedule schedule
		  	  ,to_char(start_date, 'MM.DD') startDate
		  	  ,to_char(end_date + interval '1' day, 'MM.DD') endDate
		  FROM
		  	   CALENDAR
		 WHERE
		 	   MEMBER_NO = #{memberNo}
		   AND
		   	   #{date} BETWEEN START_DATE AND END_DATE
	</select>





</mapper>